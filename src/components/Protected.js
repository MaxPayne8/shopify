import React from "react";
import { useNavigate } from "react-router";

const Protected = () => {
//   const token = localStorage.getItem("token");
//   const navigate = useNavigate();
//   return <div>{token ? navigate("/browse") : navigate("/")}</div>;
};

export default Protected;
